services:
  boot:
    image: boot
    container_name: boot
    restart: unless-stopped
    environment:
      - SERVER_PORT=8080
    networks:
      - npm_shared_net
networks:
  npm_shared_net:
    name: npm-network
    external: true
